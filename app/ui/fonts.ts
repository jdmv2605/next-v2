import { Lusitana } from 'next/font/google';
import { Inter } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

// Carga la fuente "Lusitana" de Google Fonts
export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: ['400', '700'],
});
